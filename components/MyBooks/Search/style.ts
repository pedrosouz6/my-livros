import styled from 'styled-components';

import { Theme } from '../../../theme';

const {
    beige
} = Theme.light;

export const ContainerSearch = styled.div `
    width: 100%;
    display: flex;
    gap: 1.5rem;
`